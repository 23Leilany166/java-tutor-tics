import java.util.Scanner;

public class CalculoTangente {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Ingrese el valor del seno: ");
        double seno = scanner.nextDouble();

        System.out.print("Ingrese el valor del coseno (diferente de 0): ");
        double coseno = scanner.nextDouble();

        // Verificar que el coseno sea diferente de 0
        if (coseno != 0) {
            // Calcular la tangente
            double tangente = seno / coseno;

            System.out.println("La tangente del ángulo es: " + tangente);
        } else {
            System.out.println("El valor del coseno debe ser diferente de 0.");
        }
    }
}
