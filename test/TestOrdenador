import java.util.Arrays;

public class TestOrdenador {
    public static void main(String[] args) {
        int[] arr = {5, 1, 4, 2, 8};
        int[] esperado = {1, 2, 4, 5, 8};
        
        Ordenador.ordenarPorSeleccion(arr);
        
        if (Arrays.equals(arr, esperado)) {
            System.out.println("✅ Test Ordenador PASÓ");
        } else {
            System.out.println("❌ Test Ordenador FALLÓ: " + Arrays.toString(arr));
        }
    }
}