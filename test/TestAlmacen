public class TestAlmacen {
    public static void main(String[] args) {
        Almacen alm = new Almacen();
        alm.insertar(5);
        alm.insertar(10);
        alm.insertar(5);
        
        // Eliminar 5. Debería quedar [10, 5] (elimina el primero)
        boolean eliminado = alm.eliminar(5);
        
        // Verificar contenido accediendo al ArrayList interno o toString
        String contenido = alm.getElementos().toString();
        
        if (eliminado && contenido.equals("[10, 5]")) {
            System.out.println("✅ Test Almacen PASÓ");
        } else {
            System.out.println("❌ Test Almacen FALLÓ: " + contenido);
        }
    }
}